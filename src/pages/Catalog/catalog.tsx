import styles from './catalog.module.css';

export const Catalog = () => {
   return (
      <div className={styles.wrapper}>
         <p className={styles.title}>Здесь позже будет каталог товаров</p>
      </div>
   );
};
