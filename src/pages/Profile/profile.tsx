import styles from './profile.module.css';

export const Profile = () => {
   return (
      <div className={styles.wrapper}>
         <p className={styles.title}>Здесь позже будет регистрация</p>
      </div>
   );
};
