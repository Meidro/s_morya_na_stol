import styles from './search.module.css';

export const Search = () => {
   return (
      <div className={styles.wrapper}>
         <p className={styles.title}>Здесь позже будет поиск</p>
      </div>
   );
};
